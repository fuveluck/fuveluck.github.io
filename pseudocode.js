
// Функція для скулемізації
function skolemize(ast, universalVars = []) {
    // Для екзистенційного квантора генеруємо функцію з аргументами
    // із універсальних змінних, під якими знаходиться квантор
}

// Функція для розподілу диз'юнкцій над кон'юнкціями
function distributeDisjunctions(ast) {
    // Застосовуємо дистрибутивність і логічні тотожності
}

// Функція для заміни символів
function symbolChange(ast) {
    // Змінні: перша літера велика
    // Функції та предикати: перша літера мала
}

// Функція для форматування формули в CNF
function formatCNF(ast) {
    // Формуємо список диз'юнктів для використання в резолюції
}